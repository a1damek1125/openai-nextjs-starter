"""Finalis Portal — the working application shell around the tested core.

Persistence (SQLite dev / PostgreSQL prod path) + REST API (FastAPI) +
auth/RBAC + server-rendered UI + provider mocks behind real interfaces.
"""
